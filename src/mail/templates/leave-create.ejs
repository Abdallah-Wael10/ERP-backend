<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Leave Request Submitted</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 0; padding: 20px; background-color: #f4f4f4; }
        .container { max-width: 600px; margin: 0 auto; background-color: white; padding: 20px; border-radius: 10px; box-shadow: 0 0 10px rgba(0,0,0,0.1); }
        .header { background-color: #ffc107; color: #333; padding: 20px; text-align: center; border-radius: 10px 10px 0 0; margin: -20px -20px 20px -20px; }
        .leave-info { background-color: #f8f9fa; padding: 15px; border-radius: 5px; margin: 20px 0; }
        .status-badge { display: inline-block; padding: 5px 15px; border-radius: 20px; background-color: #ffc107; color: #333; font-weight: bold; margin: 10px 0; }
        .footer { text-align: center; margin-top: 30px; color: #666; }
        .calendar-icon { font-size: 48px; color: #ffc107; text-align: center; margin: 20px 0; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>📝 Leave Request Submitted</h1>
            <p>Your request is under review</p>
        </div>

        <div class="calendar-icon">
            🗓️
        </div>

        <div class="leave-info">
            <h3>👤 Employee Information</h3>
            <p><strong>Name:</strong> <%= employee.fullName %></p>
            <p><strong>Email:</strong> <%= employee.email %></p>
            <p><strong>Role:</strong> <%= employee.role.toUpperCase() %></p>
        </div>

        <div class="leave-info">
            <h3>📅 Leave Details</h3>
            <p><strong>Start Date:</strong> <%= new Date(startDate).toLocaleDateString('en-US', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }) %></p>
            <p><strong>End Date:</strong> <%= new Date(endDate).toLocaleDateString('en-US', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }) %></p>
            <p><strong>Total Days:</strong> <%= totalDays %> day<%= totalDays > 1 ? 's' : '' %></p>
            <p><strong>Status:</strong> <span class="status-badge">⏳ PENDING</span></p>
        </div>

        <div class="leave-info">
            <h3>📋 Reason</h3>
            <p><%= reason %></p>
        </div>

        <div style="background-color: #fff3cd; padding: 15px; border-radius: 5px; margin: 20px 0;">
            <h4>🔄 What's Next?</h4>
            <p>• Your leave request has been submitted successfully</p>
            <p>• HR team will review your request</p>
            <p>• You'll receive an email notification once it's reviewed</p>
        </div>

        <div style="background-color: #e3f2fd; padding: 15px; border-radius: 5px; margin: 20px 0;">
            <h4>💡 Note:</h4>
            <p>You can track the status of your leave request in your dashboard</p>
        </div>

        <div class="footer">
            <p>ERP System | Leave Management</p>
            <p style="font-size: 12px;">This is an automated notification</p>
        </div>
    </div>
</body>
</html>